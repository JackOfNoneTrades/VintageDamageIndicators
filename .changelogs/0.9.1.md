* Added more built-in mob override presets for supported mods
* Improved HUD rendering compatibility for Special Mobs entities with randomized sizes
* The config override editor now stages changes until saved, (manage with  `Save` and `Cancel` buttons), and can be opened directly with the `/vdioverrides` command
* Ingame HUD name overrides now work correctly and they also get passed to the localizer function
* Added Chinese localization by @Omgise
* Added an interactive python script for merging override jsons (unix only)
